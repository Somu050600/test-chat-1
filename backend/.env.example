# Firebase service account JSON (single line, stringified)
FIREBASE_SERVICE_ACCOUNT=

# Deprecated: notify/api now uses Firebase ID tokens (Authorization: Bearer).
# API_SECRET_KEY is no longer used by api/notify.js.
API_SECRET_KEY=
