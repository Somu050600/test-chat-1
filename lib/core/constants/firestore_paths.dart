/// Firestore collection / field names (services use these in later steps).
abstract final class FirestorePaths {
  static const users = 'users';
  static const conversations = 'conversations';
  static const messages = 'messages';
}
