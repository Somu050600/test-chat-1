/// User-visible copy centralized for consistency across features.
abstract final class AppStrings {
  static const appTitle = 'Chat';
  static const homeSubtitle =
      'Firebase initialized. Architecture ready. Next: Google sign-in (Step 3).';
  static const loginPlaceholder = 'Google sign-in — Step 3';
  static const chatPlaceholder = '1:1 chat — Step 5';
}
